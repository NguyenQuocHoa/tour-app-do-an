import React from "react";

const OrderList = props => {
	return (
		<div class="container py-5 mt-5">
			<div class="row">
				<h1>Danh sách đơn hàng </h1>
			</div>
		</div>
	);
};

export default OrderList;

const OrderRecord = ({ order }) => {
	return <div>tour record</div>;
};
